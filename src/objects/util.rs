use crate::random::Random;

pub struct ScrollNameService {
    pub scroll_adj: Vec<String>
}

impl ScrollNameService {
    pub fn init(&mut self, random: &mut Random, /* randname: Randname, when it gets implemented */) -> () {

    }
}

